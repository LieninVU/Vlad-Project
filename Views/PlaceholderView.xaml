<UserControl x:Class="ForVlad.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Background="#F8F9FA">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
            <TextBlock Text="{Binding Icon}" FontSize="56" HorizontalAlignment="Center" Foreground="#BDC3C7"/>
            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"
                       HorizontalAlignment="Center" Margin="0,16,0,8" TextWrapping="Wrap" TextAlignment="Center"/>
            <TextBlock Text="{Binding Message}" FontSize="14" Foreground="#7F8C8D"
                       TextWrapping="Wrap" TextAlignment="Center" LineHeight="22"/>
        </StackPanel>
    </Grid>
</UserControl>
