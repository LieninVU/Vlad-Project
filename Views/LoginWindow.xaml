<Window x:Class="ForVlad.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ForVlad.Views"
        xmlns:behaviors="clr-namespace:ForVlad.Behaviors"
        xmlns:converters="clr-namespace:ForVlad.Converters"
        mc:Ignorable="d"
        Title="LeasingSystem - Вход" Height="400" Width="500"
        MinWidth="400" MinHeight="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F0F0F0"
        behaviors:WindowScalingBehavior.IsEnabled="True"
        behaviors:WindowScalingBehavior.BaseWidth="500"
        behaviors:WindowScalingBehavior.BaseHeight="400">
    
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style TargetType="Button" x:Key="SecondaryButton">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2C3E50">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="LeasingSystem" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="Вход в систему" Foreground="#BDC3C7" FontSize="14"/>
            </StackPanel>
        </Border>
        
        <Grid Grid.Row="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Логин:" VerticalAlignment="Bottom"/>
            <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     ToolTip="Введите ваш логин"/>
            
            <TextBlock Grid.Row="2" Text="Пароль:" VerticalAlignment="Bottom"/>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <PasswordBox Grid.Column="0" x:Name="PasswordBox" 
                            behaviors:PasswordBoxBehavior.BoundPassword="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                            ToolTip="Введите ваш пароль"/>
                
                <TextBox Grid.Column="0" x:Name="VisiblePasswordBox" 
                         Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                         Visibility="Collapsed"
                         ToolTip="Введите ваш пароль"/>
                
                <CheckBox Grid.Column="1" 
                          Content="👁" 
                          FontSize="16"
                          Margin="5,0,0,0"
                          ToolTip="Показать пароль"
                          Checked="ShowPassword_Checked"
                          Unchecked="ShowPassword_Unchecked"/>
            </Grid>
            
            <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" 
                       Foreground="#E74C3C" 
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" 
                       Margin="0,5,0,0" 
                       TextWrapping="Wrap"/>
            
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Войти" Command="{Binding LoginCommand}" Width="120"/>
            </StackPanel>
        </Grid>
        
        <Border Grid.Row="2" Background="#ECF0F1" Padding="10">
            <TextBlock Text="© LeasingSystem 2026" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
